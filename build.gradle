// Top-level build file where you can add configuration options common to all sub-projects/modules.

buildscript {
    ext {
        compileSdkVersion = 29
        minSdkVersion = 21
        targetSdkVersion = 29

        kotlinVersion = '1.3.21'

        junitVersion = '4.13-beta-2'
        testRunnerVersion = '1.2.0-beta01'
        espressoVersion = '3.2.0-beta01'

        androidX = [
                annotation           : '1.1.0', // done
                appcompat            : '1.1.0-beta01', // done
                archCore             : '2.1.0-beta01',// done
                biometric            : '1.0.0-alpha04', //done
                contstraintlayout    : '2.0.0-beta2', // done
                coordinatorlayout    : '1.1.0-beta01', // done
                core                 : '1.1.0-rc01',
                drawerlayout         : '1.0.0', // done
                fragment             : '1.1.0-beta01', // done
                lifecycle            : '2.2.0-alpha01', // done
                navigation           : '2.1.0-alpha05', // done
                palette              : '1.0.0', // done
                preference           : '1.1.0-beta01', // done
                recyclerview         : '1.1.0-alpha06', // done
                recyclerviewSelection: '1.1.0-alpha06', // done
                transition           : '1.1.0-rc02',
                vectorDrawable       : '1.1.0-beta02',
                viewpager2           : '1.0.0-alpha05'
        ]

        playVersion = [
                core  : '1.6.1',
                vision: '18.0.0'
        ]

        takisoftVersion = [
                preferencex: '1.1.0-alpha05',
                colorpicker: '1.0.0'
        ]

        materialComponentsVersion = '1.1.0-alpha07'

        libVersions = [
                sqlCipher                : '4.2.0',
                requery                  : '1.6.1',
                sqlite                   : '3.28.0',
                dagger                   : '2.19',
                rxAndroid                : '2.1.1',
                rxJava                   : '2.2.8',
                timber                   : '4.7.1',
                dotsindicator            : '3.0.3',
                circularProgressIndicator: '1.3.0',
                zip4j                    : '1.3.3',
                zxing                    : '3.3.3',
                gson                     : '2.8.5',
                licensesdialog           : '2.1.0'
        ]
    }

    repositories {
        google()
        jcenter()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:3.5.0-beta05'
        classpath "androidx.navigation:navigation-safe-args-gradle-plugin:${androidX.navigation}"
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:$kotlinVersion"


        // NOTE: Do not place your application dependencies here; they belong
        // in the individual module build.gradle files
    }
}

allprojects {
    repositories {
        google()
        jcenter()
        mavenCentral()
        maven { url 'https://jitpack.io' }
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
